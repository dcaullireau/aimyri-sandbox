# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - 3a8a5953-b901-5550-9345-ff4b105b7f9c

on:
  push:
    branches:
      - v2-sandbox
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://sandboxcontainerregistries.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_c1ea0bbf4e264f31a8a9405fac42abdf }}
        password: ${{ secrets.AzureAppService_ContainerPassword_8d30be206f724ef59ef989a1416a418e }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: sandboxcontainerregistries.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_c1ea0bbf4e264f31a8a9405fac42abdf }}/sandboxcontainerregistries.azurecr.io/aimyri-sandbox:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: '3a8a5953-b901-5550-9345-ff4b105b7f9c'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_438b31187da148e7837578204e8aedc4 }}
        images: 'sandboxcontainerregistries.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_c1ea0bbf4e264f31a8a9405fac42abdf }}/sandboxcontainerregistries.azurecr.io/aimyri-sandbox:${{ github.sha }}'